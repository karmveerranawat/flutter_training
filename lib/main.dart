import 'package:flutter/material.dart';
import 'home_page/home_ui.dart';

main() {
  runApp(HotReload());
}

class HotReload extends StatelessWidget {
  build(BuildContext structureState) {
    return mainApp();
  }
}
